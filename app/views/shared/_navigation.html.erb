<% if user_signed_in? %>
  <!-- <li>
  </li> -->
  <li class='dropdown'>
    <a class='dropdown-toggle' data-toggle='dropdown' href='#'>
      Settings
      <b class='caret'></b>
    </a>
    <ul class='dropdown-menu'>
      <% if current_user && current_user.mapping.present? && !current_user.mapping.new_record? %>  
        <li><%= link_to "Mapping fields", mapping_path(current_user.mapping) %></li>
      <% else %>
        <li><%= link_to "Mapping fields", new_mapping_path %></li>
      <% end %>
      <%# if current_user && current_user.cardinfo.present? && !current_user.cardinfo.new_record?  %>  
       <li><%= link_to "Dashboard", :controller=>"dashboard", :action=>"index" %></li>
      
      <%# if current_user && current_user.ping.present? && !current_user.ping.new_record? %>  
        <!-- <li><%#= link_to "Ping request", ping_path(current_user.ping) %></li> -->
      <%# end %>
      
    </ul>
  </li>
  <li class='dropdown'>
    <a class='dropdown-toggle' data-toggle='dropdown' href='#'>
      Account
      <b class='caret'></b>
    </a>
    <ul class='dropdown-menu'>
      <% @plans = Plan.all %>
      <% if @plans.present?%>
        <li><%= link_to "Plans", plans_path %></li>
      <% end %>
       <li><%= link_to "Billing", billings_path %></li>
    </ul>
  </li>
  <% if current_user && current_user.user_type == "admin" %>
    <li>
      <%= link_to "Add Plan", new_plan_path %>
    </li>
  <% end %>
   <li class=
      <% if current_user && current_user.user_type=="admin" %>
      "dropdown pull-right offset4"
      <% else %>
      "dropdown pull-right offset6"
      <% end %>> 
  <!-- <li class='dropdown pull-right offset5'> -->
    <a class='dropdown-toggle' data-toggle='dropdown' href='#'>
      <%= current_user.user_name.capitalize %>
      <b class='caret'></b>
    </a>
    <ul class='dropdown-menu'>
      <li><%= link_to "Log out", destroy_user_session_path, :method => :delete %></li>
    </ul>
  </li>
<% end %>
